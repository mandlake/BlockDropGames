# 🎮 React BlockDropGame Game

React + Vite + TypeScript 기반으로 제작한 **오리지널 블록 드롭 퍼즐 게임**입니다.  
클래식 테트리스가 아닌, **N×N 랜덤 블록 + TGM 스타일 속도 곡선 + 레벨/점수 시스템 + 설정 가능한 게임 규칙**을 특징으로 하는 연습·포트폴리오용 프로젝트입니다.

---

## 🚀 데모 링크

👉 **[BlockDropGame 플레이하기](https://block-drop-games.vercel.app/)**  
(브라우저에서 바로 실행 가능합니다)

---

## 📌 주요 특징 요약

- 테트리스 IP를 회피한 **완전 오리지널 블록 생성 규칙**
- 한 판마다 고정되는 **랜덤 블록 셋**
- **이번 판 블록 미리보기(Prototype Panel)** 제공
- **Pause / Settings / Game Over**를 오버레이 UI로 통합
- 게임 규칙을 직접 조절할 수 있는 **설정 모달**

---

## 🎮 게임 플레이

- 좌우 이동 / 회전 / 한 칸 내리기
- **하드드롭 (Space)**
- 다음 블록(Next Piece) 미리보기
- 줄 완성 시 자동 삭제
- **일시정지(Pause) / 재개**
- 게임 종료 시 결과 화면 표시

---

## 🧩 블록 규칙 & 랜덤 생성

- **테트리스 IP를 회피하기 위한 오리지널 블록 시스템**
- 한 판 시작 시:
  - `SHAPE_TYPE_COUNT` 개수만큼 **서로 다른 랜덤 블록 모양**을 생성
  - 각 블록은 `N×N`(정사각형) 형태이며, N 범위는 설정값으로 제어
  - 각 블록 내부의 칸 수(채워진 블록 수)도 최소/최대 범위 안에서 랜덤 생성

- 생성된 블록은:
  - **연결 그래프 형태**로만 생성 (한 덩어리)
  - 회전/정규화했을 때 **클래식 테트로미노 7종과 일치하는 패턴은 자동 제외**

- 이렇게 만들어진 블록 셋은:
  - **해당 판 전체 동안 고정**
  - 같은 타입의 블록은 언제나 같은 모양으로 등장

> 즉, 매 플레이마다 블록 셋은 달라지지만,  
> 한 판 안에서는 패턴을 학습하며 플레이할 수 있도록 설계했습니다.

---

## 🔍 이번 판 블록 셋 시각화 패널

- 화면 레이아웃은 다음과 같이 3분할입니다.

  ```text
  [1] 이번 판 블록 목록 패널
  [2] 메인 게임 보드
  [3] 점수 / 다음 블록 / 조작법 패널
  ```

---

## 🧱 PrototypePanel – 이번 판 블록을 한눈에 확인

이 패널은 **해당 판에서 등장할 수 있는 모든 블록 모양을 미니맵 형태로 시각화**합니다.

- 게임 시작 시 `SHAPE_TYPE_COUNT` 개수만큼 생성된 랜덤 블록 프로토타입을 그대로 표시
- 각 블록은 다음 정보로 구성됩니다:
  - **타입 번호(#1, #2, #3 …)**
  - **블록 모양 미리보기(최소화된 Grid 렌더링)**

- 배치 방식은 세로(column) 방식이며,
  - ```bash

    [#번호] [미니 블록 이미지]
    [#번호] [미니 블록 이미지]
    ...

    ```

  - 형태로 나란히 정렬됩니다.

이 구성 덕분에 플레이어는:

- 이번 판에서 **어떤 블록들이 등장하는지 즉시 파악**
- 각 블록의 **형태·비율·회전 시 외형**을 미리 익힐 수 있고
- 플레이 중 등장한 블록이 **프로토타입 중 몇 번 타입인지 바로 식별**

할 수 있습니다.

---

## 🎯 왜 중요한가?

전통적인 테트리스 계열은 **블록 구조가 고정**되어 있지만,  
이 BlockDropGame은 **플레이마다 완전히 새로운 블록 세트가 생성**됩니다.

PrototypePanel은 그 차이를 직관적으로 보완해 줍니다.

- “이번 판은 어떤 모양의 블록들인가?”
- “각 모양이 얼마나 길고, 얼마나 복잡한가?”
- “회전하면 어떤 패턴으로 변하는가?”

등을 사전에 확인함으로써,

> **게임 중 갑작스럽게 낯선 형태가 나와도 적응할 시간을 줄이고  
> 전략적 판단을 돕기 위한 핵심 UX 요소**로 작동합니다.

---

## ⚡ 난이도 시스템

- **무한 레벨 시스템**
- 클리어한 줄 수에 따라 자동 레벨 상승  
  → `linesPerLevel 줄 제거 → 레벨 +1`
- **TGM 스타일 속도 곡선**을 참고한 낙하 속도 테이블 적용
- 속도 테이블을 넘어선 레벨에서는 **최대 낙하 속도 유지**

---

## ⚙ 레벨 & 속도 (TGM 스타일)

- **5줄 클리어 → 레벨 +1**
- 레벨은 **상한 없이 무한 상승**
- 속도 테이블(SPEED_TABLE)을 기준으로 점진적 가속
- 테이블 최대치를 넘어가면 **최대 속도 유지**

예시 속도 테이블(일부):

| 레벨 | 속도(ms)       |
| ---- | -------------- |
| 1    | 800            |
| 5    | 430            |
| 10   | 230            |
| 15   | 120            |
| 20+  | 70 (최대 속도) |

실제 TGM과 1:1 동일한 값이 아니라,
**초반 학습 -> 중반 긴장감 -> 후반 폭발적인 속도** 흐름을 의도한 커스텀 곡선입니다.

---

## 📊 점수 구조

```bash

최종 점수 = 기본 점수 × 현재 레벨

```

기본 점수:

| 클리어 줄 수 | 기본 점수 |
| ------------ | --------- |
| 1줄          | 100       |
| 2줄          | 300       |
| 3줄          | 500       |
| 4줄          | 800       |

레벨이 올라갈수록 **동일한 라인 클리어도 더 높은 점수**를 주도록 설계되어 있습니다.

---

## ⏸ 일시정지(Pause) & 설정 시스템

### Pause 기능

- **ESC 키**로 언제든 일시정지/재개 가능
- PAUSE 상태 시:
  - 게임 화면 흐림 처리
  - 화면 상단 중앙에 `PAUSED` 표시
  - 자동 낙하 및 조작 입력 정지

### 설정 모달(Settings)

- 게임 화면 상단의 **설정 버튼** 또는
- Game Over 화면에서 **설정 버튼**을 통해 접근 가능
- 설정 모달이 열리면 자동으로 Pause 처리

설정에서 조절 가능한 항목 예시:

- 보도 크기 (rows/cols)
- 블록 종류 수
- 블록 크기 범위
- 블록 칸 수 범위
- 레벨업 기준 줄 수
- 키 설정 (이동 / 회전 / 드롭)

---

## 🧾 게임 결과 화면 (Game Over)

게임 오버 시, 별도의 결과 오버레이가 화면 중앙에 표시됩니다.

표시 정보:

- 최종 점수
- 도달 레벨
- 제거한 총 줄 수

하단 버튼:

- 다시 시작
- 설정

Pause/Settings와 동일한 오버레이 계열 UI를 통일해
일관된 UX를 유지하도록 설계했습니다.

---

## 🎨 UI / 시각적 요소

- 블록 타입별 고유 색상(HSL 기반, 매 판 고정)
- 고스트 피스(하드 드롭 예상 위치) 표시
- **레벨업 애니메이션 오버레이 표시**
- Pause / Game Over 시 배경 흐림 처리

---

## 🛠 기술 스택

| 기술            | 설명                                |
| --------------- | ----------------------------------- |
| **React**       | UI 렌더링 및 게임 상태 처리         |
| **TypeScript**  | 정적 타입을 통한 안정성 확보        |
| **Vite**        | 고속 개발 서버 & 번들러             |
| **CSS Grid**    | BlockDrop 보드 표현                 |
| **React Hooks** | useState / useEffect / useMemo 활용 |

---

## 🧱 프로젝트 구조

```bash

src/
├── components/              # UI 컴포넌트
│   ├── GameBoard.tsx
│   ├── ScorePanel.tsx
│   ├── NextPiecePanel.tsx
│   ├── ControlsPanel.tsx
│   ├── LevelUpOverlay.tsx
│   ├── PrototypePanel.tsx
│   └── SettingsPanel.tsx
│
├── logic/                   # 게임 규칙 / 로직
│   ├── constants.ts
│   ├── types.ts
│   ├── utils/
│   │   ├── board.ts
│   │   ├── collision.ts
│   │   ├── lines.ts
│   │   ├── rotation.ts
│   │   └── shapeGenerator.ts
│   └── index.ts
│
├── styles/                  # 전역 스타일
│   ├── App.css              # CSS 엔트리
│   ├── layout.css
│   ├── blockDrop.css
│   ├── sidePanels.css
│   ├── nextPiece.css
│   ├── protoPanels.css
│   ├── levelUp.css
│   ├── pause.css
│   ├── gameOver.css
│   └── modal.css
│
├── App.tsx                  # 최상위 상태 관리
└── main.tsx                 # React 엔트리


```

---

## 🎮 조작법

| 키        | 기능                       |
| --------- | -------------------------- |
| ← / →     | 좌우 이동                  |
| ↓         | 한 칸 내리기               |
| ↑         | 회전                       |
| **Space** | **하드드롭**               |
| **ESC**   | **일시정지 / 재개**        |
| (자동)    | 레벨에 따른 낙하 속도 증가 |

---

## 📦 설치 및 실행

```bash
# 1. 프로젝트 클론
git clone https://github.com/your-username/BlockDropGame-react.git
cd BlockDropGame-react

# 2. 패키지 설치
npm install

# 3. 개발 서버 실행
npm run dev
```

---
